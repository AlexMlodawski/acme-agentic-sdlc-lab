{
  "name": "@acme/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Local zero-secret browser acceptance for the Acme Agentic SDLC Lab.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "playwright test",
    "test:built": "playwright test --config playwright.built.config.ts",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "26.4.0",
    "typescript": "5.9.3"
  }
}
