# Bob IDE stage 1 continuation — build and validate locally

Go with the reviewed plan, but this authorization covers local/read-only work
only. It does not authorize any WXO tenant write.

From `agents/store_support_agent`:

1. show `uv run orchestrate --version` and require ADK `2.15.0`;
2. show `uv run orchestrate env list` and identify the active remote environment;
3. if authentication has expired, pause and ask me to run
   `uv run orchestrate env activate <environment-name>` in the interactive terminal;
   never accept or type the key in Bob chat;
4. run `uv run orchestrate models list --raw` with UTF-8 console output;
5. choose an exact supported model returned by that tenant, preferring its
   marked default model, and explain the choice in one sentence;
6. materialize the template to
   `.generated/store_support_agent.yaml` with the repository script;
7. run `npm run verify:agent` from the repository root; and
8. inspect Draft names with read-only list commands and report whether each
   target is absent, identical-looking, or a potential collision.

Do not edit the checked-in agent YAML, import/update/remove any tenant resource,
configure credentials, deploy to Live, commit, or push. Report exact commands,
sanitized results, the selected model ID, and the next approval boundary. Stop
before the first tenant write.
